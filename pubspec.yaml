name: video_recorder

description: Online exam proctoring recorder application.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.0+2
  dio: ^5.7.0
  ffmpeg_kit_flutter_new_video: ^3.2.0
  flutter_secure_storage: ^9.2.2
  path_provider: ^2.1.4
  permission_handler: ^11.3.1
  provider: ^6.1.2

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
