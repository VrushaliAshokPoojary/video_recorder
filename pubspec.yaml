name: video_recorder
description: Production-ready Flutter proctoring recorder demo.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.0+2
  dio: ^5.7.0
  flutter_ffmpeg: ^0.4.2
  path: ^1.9.0
  path_provider: ^2.1.4
  permission_handler: ^11.3.1
  uuid: ^4.5.1
  flutter_screen_recording: ^2.0.17

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
